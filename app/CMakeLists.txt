set(APP_HDRS ${APP_HDRS} ${CMAKE_CURRENT_SOURCE_DIR})

set(APP_SRCS ${APP_SRCS} ${CMAKE_CURRENT_SOURCE_DIR}/main.cpp)
set(APP_SRCS ${APP_SRCS} ${CMAKE_CURRENT_SOURCE_DIR}/CtrlCoreModule.cpp)

set(APP_HDRS ${APP_HDRS} PARENT_SCOPE)
set(APP_SRCS ${APP_SRCS} PARENT_SCOPE)
